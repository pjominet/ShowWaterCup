CREATE TABLE [swc].[Position] (
    [PositionId] INT NOT NULL,
    [X]          INT NOT NULL,
    [Y]          INT NOT NULL,
    CONSTRAINT [positionId_pk] PRIMARY KEY CLUSTERED ([PositionId] ASC)
);

