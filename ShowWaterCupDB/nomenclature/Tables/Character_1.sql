CREATE TABLE [nomenclature].[Character] (
    [CharacterId] INT           NOT NULL,
    [Name]        VARCHAR (100) NULL,
    CONSTRAINT [characterId_pk] PRIMARY KEY CLUSTERED ([CharacterId] ASC)
);

